LEGACY_HTML = True  # 既存HTMLを保持（再アセンブル禁止）
TITLE = '手芸材料費計算【無料】作品コスト・材料リストを管理'
DESCRIPTION = '手芸・クラフト作品の材料費を自動計算。販売価格の設定や材料リスト管理に役立つ無料ツール。'
JS_CODE = 'const PRESETS = {\n    knit: [\n      { name: \'毛糸（並太）\', price: 800, buyQty: 100, buyUnit: \'g\', useQty: 80, useUnit: \'g\' },\n      { name: \'棒針（2本セット）\', price: 400, buyQty: 1, buyUnit: \'セット\', useQty: 0.1, useUnit: \'セット\' },\n    ],\n    sewing: [\n      { name: \'綿布（110cm幅）\', price: 700, buyQty: 100, buyUnit: \'cm\', useQty: 60, useUnit: \'cm\' },\n      { name: \'糸\', price: 200, buyQty: 1, buyUnit: \'巻\', useQty: 0.3, useUnit: \'巻\' },\n      { name: \'接着芯\', price: 300, buyQty: 100, buyUnit: \'cm\', useQty: 40, useUnit: \'cm\' },\n      { name: \'持ち手テープ\', price: 250, buyQty: 200, buyUnit: \'cm\', useQty: 80, useUnit: \'cm\' },\n    ],\n    resin: [\n      { name: \'UVレジン液\', price: 1200, buyQty: 30, buyUnit: \'ml\', useQty: 5, useUnit: \'ml\' },\n      { name: \'モールド\', price: 500, buyQty: 1, buyUnit: \'個\', useQty: 0.05, useUnit: \'個\' },\n      { name: \'カラーフィルム・押し花\', price: 300, buyQty: 1, buyUnit: \'セット\', useQty: 0.2, useUnit: \'セット\' },\n      { name: \'UVライト\', price: 2000, buyQty: 1, buyUnit: \'個\', useQty: 0.01, useUnit: \'個\' },\n    ],\n    embroidery: [\n      { name: \'刺繍糸（25番手）\', price: 100, buyQty: 8, buyUnit: \'m\', useQty: 3, useUnit: \'m\' },\n      { name: \'刺繍用布\', price: 300, buyQty: 1, buyUnit: \'枚\', useQty: 1, useUnit: \'枚\' },\n      { name: \'刺繍針\', price: 300, buyQty: 1, buyUnit: \'セット\', useQty: 0.05, useUnit: \'セット\' },\n      { name: \'刺繍枠\', price: 500, buyQty: 1, buyUnit: \'個\', useQty: 0.02, useUnit: \'個\' },\n    ],\n    candle: [\n      { name: \'キャンドルワックス\', price: 1500, buyQty: 500, buyUnit: \'g\', useQty: 100, useUnit: \'g\' },\n      { name: \'芯\', price: 300, buyQty: 10, buyUnit: \'本\', useQty: 1, useUnit: \'本\' },\n      { name: \'香料（フレグランスオイル）\', price: 800, buyQty: 30, buyUnit: \'ml\', useQty: 5, useUnit: \'ml\' },\n      { name: \'容器（ガラスカップ）\', price: 200, buyQty: 1, buyUnit: \'個\', useQty: 1, useUnit: \'個\' },\n    ],\n  };\n\n  let rows = [];\n\n  function loadPreset(key) {\n    rows = PRESETS[key].map((r, i) => ({ ...r, id: Date.now() + i }));\n    renderRows();\n  }\n\n  function addRow() {\n    rows.push({ id: Date.now(), name: \'\', price: 0, buyQty: 1, buyUnit: \'個\', useQty: 1, useUnit: \'個\' });\n    renderRows();\n  }\n\n  function removeRow(id) {\n    rows = rows.filter(r => r.id !== id);\n    renderRows();\n  }\n\n  function renderRows() {\n    document.getElementById(\'materialRows\').innerHTML = rows.map(r => `\n      <div class="material-row" id="row-${r.id}">\n        <input type="text" placeholder="材料名" value="${r.name}" oninput="updateRow(${r.id},\'name\',this.value)" aria-label="材料名">\n        <input type="number" placeholder="購入単価(円)" value="${r.price}" min="0" oninput="updateRow(${r.id},\'price\',+this.value)" aria-label="購入単価(円)">\n        <input type="number" placeholder="購入量" value="${r.buyQty}" min="0.01" step="0.01" oninput="updateRow(${r.id},\'buyQty\',+this.value)" aria-label="購入量">\n        <input type="number" placeholder="使用量" value="${r.useQty}" min="0" step="0.01" oninput="updateRow(${r.id},\'useQty\',+this.value)" aria-label="使用量">\n        <button class="remove-btn" onclick="removeRow(${r.id})">✕</button>\n      </div>`).join(\'\');\n    if (!rows.length) {\n      document.getElementById(\'materialRows\').innerHTML = \'<p style="color:#888;font-size:0.85rem;padding:0.5rem;">上のプリセットから選ぶか、「＋ 材料を追加」でスタート</p>\';\n    }\n  }\n\n  function updateRow(id, field, value) {\n    rows = rows.map(r => r.id === id ? { ...r, [field]: value } : r);\n  }\n\n  function calculate() {\n    const qty = parseInt(document.getElementById(\'productionQty\').value) || 1;\n    const hours = parseFloat(document.getElementById(\'workHours\').value) || 0;\n    const hourly = parseFloat(document.getElementById(\'hourlyRate\').value) || 0;\n    const details = rows.map(r => {\n      const costPerUnit = r.price / (r.buyQty || 1);\n      const itemCost = costPerUnit * r.useQty;\n      return { name: r.name || \'（未入力）\', cost: itemCost };\n    });\n    const totalMat = details.reduce((s, d) => s + d.cost, 0);\n    const perItem = totalMat / qty;\n    const laborCost = (hours * hourly) / qty;\n    const totalWithLabor = perItem + laborCost;\n    document.getElementById(\'totalMateri'
MAIN_HTML = '<div><button class="btn">開始する</button></div>'
FAQ = [
    ('手芸材料費計算は無料で使えますか？', 'はい、完全無料・登録不要でご利用いただけます。'),
    ('何回でも使えますか？', 'はい、回数制限なく何度でもご利用いただけます。'),
    ('入力したデータはサーバーに送信されますか？', 'いいえ。すべての処理はブラウザ内で完結し、入力内容はサーバーへ送信されません。'),
    ('スマートフォンでも使えますか？', 'はい、スマートフォン・タブレット・PCすべてに最適化されています。'),
    ('結果を保存・共有できますか？', 'スクリーンショットでの保存またはSNSシェアボタンからご共有いただけます。'),
]
HOW_TO = [
    'ページを開き、入力フォームの項目を確認する',
    '必要な情報を入力または選択する',
    '実行ボタンをクリックして結果を取得する',
    '表示された結果・アドバイスを確認する',
    '必要に応じてコピー・SNSシェアで活用する',
]
FOOTER_LINKS = [('https://appadaycreator.com/sleep-quality-checker/', '睡眠の質チェッカー'), ('https://appadaycreator.com/bmi-body-tracker/', 'BMI・体重管理'), ('https://appadaycreator.com/household-budget-analyzer/', '家計簿診断')]
